<div class="container py-5">
  <div class="row">
    <div class="col-12 col-md-6 offset-lg-3">
      <div class="bg-white rounded p-3 shadow">

        <h1>Pet Details</h1>

          <div>
            <strong>Name:</strong>
              <%= @pet.name %>
                </div>
                <div>
                <strong>Species:</strong>
                  <%= @pet.species %>
                  </div>
<%# <div>
  <strong>Age:</strong>
  <%= @pet.age %>
                    <div>
                    <strong>Breed:</strong>
                  <%= @pet.breed %>
                </div>
            <div>
          <strong>Description:</strong>
        <%= @pet.description %>
      </div>

    <%= link_to 'Back', pets_path %>
  </div>
</div>
</div>
</div>

<%#= render 'bookings/form', pet: @pet %>
